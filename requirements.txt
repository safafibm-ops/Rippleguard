# RippleGuard prototype dependencies.
# Deliberately small: the analysis is graph maths and HTTP, nothing more.
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
networkx>=3.2
pydantic>=2.5
pytest>=8.0          # tests only
